# 左右通信の必須設定
CONFIG_ZMK_SPLIT=y
CONFIG_ZMK_SPLIT_BLE=y

# macOSの切断バグ回避
CONFIG_BT_CTLR_PHY_2M=n

# スリープ機能を完全OFF
CONFIG_ZMK_SLEEP=n

# 不要な機能のオフ
CONFIG_ZMK_DISPLAY=n
CONFIG_ZMK_KEYBOARD_NAME="Crosses"

# ⚡ Bluetooth電波出力を最大(+8dBm)にする（途切れ防止の最重要設定）
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
